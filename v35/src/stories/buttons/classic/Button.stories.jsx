import { Button } from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    size: "medium",
    backgroundColor: "#f17295",
    label: "test?"
  }
};